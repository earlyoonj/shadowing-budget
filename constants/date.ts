const DAYS = [
    'sunday',
    'monday',
    'tuesday',
    'wednesday',
    'thursday',
    'friday',
    'saturday',
];

const MONTH_DAYS = [...Array(28).keys()].map((i) => i + 1);

export { DAYS, MONTH_DAYS };
